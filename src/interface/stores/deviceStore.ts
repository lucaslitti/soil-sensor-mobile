/** Presentation DTO store. The feature implementation remains behind this adapter during migration. */
export { useDashboardStore as useDeviceStore, useDashboardStore } from '../../features/dashboard/hooks/dashboardStore';
